---
import { Icon } from "astro-icon";
import socialData from "../data/socialData.json";
import Card from "./Card.astro";
---

{
  socialData.map((social) => (
    <Card href={social.link} target="_blank" classes="p-2">
      <Icon name={social.icon} width={20} />
    </Card>
  ))
}
